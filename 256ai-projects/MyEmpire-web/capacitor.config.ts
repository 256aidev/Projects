import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.empire256.kingpin',
  appName: 'My Empire Kingpin',
  webDir: 'dist'
};

export default config;
